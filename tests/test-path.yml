name: Test path input
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Run latexmk-action with explicit path and ls cmd
        id: action
        uses: .
        with:
          path: tests/fixtures/latex
          cmd: ls
          opts: '-al'
      - name: Assert action completed successfully
        run: |
          echo "PASS: action ran ls -al in the specified path directory"
